---
interface Props {
  tag: string;
}
const { tag } = Astro.props;
---

<a
  href={`/blog/tag/${encodeURIComponent(tag)}`}
  class="font-mono text-xs text-faint no-underline transition-colors hover:text-accent"
>
  #{tag}
</a>
