---
title: "Semantic Search & Insights Engine"
summary: "Hybrid semantic + keyword retrieval over large review and feedback datasets."
problem: "Keyword search missed meaning; pure vector search missed exact matches and was costly at scale."
approach: "Hybrid retrieval on OpenSearch combining BM25 keyword scoring with vector similarity, tuned for relevance and latency at scale."
stack: ["OpenSearch", "Vectors", "Python", "AWS"]
result: "Relevant, fast retrieval across large datasets powering downstream insights."
learnings: "Hybrid beats either approach alone — keyword anchors precision, vectors add recall and meaning."
order: 4
featured: false
---

The boring truth of search: the best system is usually a careful blend, not a single clever
algorithm.
